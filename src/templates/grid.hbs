<div class="race{{class race}}">
  <p class="type">{{race}}</p>
  <div class="hover-grid">
    {{#each options}}
      <p class="opt">{{shortName}}</p>
    {{/each}}
  </div>
  <div class="reporting">
    <p>{{reporting}}% reporting</p>
  </div>
</div>
